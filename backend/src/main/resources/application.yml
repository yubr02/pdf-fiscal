server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartpos_pdv?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: America/Sao_Paulo

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173,file://
